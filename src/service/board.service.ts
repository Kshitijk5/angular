import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root',
})
export class BoardService {
  // cards = [];
  // constructor(private board: BoardService) {
  //   this.cards = [];
  // }
}
